import SpotifyAPI from './spotify';
import { Auth } from './spotify';
import * as types from './types/types';
import * as scopes from './types/scope';

export { SpotifyAPI };
export { Auth };
export { types, scopes };
